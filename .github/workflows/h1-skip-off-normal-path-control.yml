name: H1 Base Pivot Public Signal Slow

on:
  pull_request:
    branches:
      - h1-basepivot-publicsignal-A-20260812

jobs:
  safety:
    name: H1 skip-off normal-path control
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hold observable finalization window
        run: sleep 45
      - name: Pass
        run: echo H1_BASEPIVOT_PUBLIC_SIGNAL_OK_20260812
