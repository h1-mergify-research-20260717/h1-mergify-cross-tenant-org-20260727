name: H1 Base Pivot Recheck Slow

on:
  pull_request:
    branches:
      - h1-basepivot-recheck-A-20260812

jobs:
  safety:
    name: H1 skip-off normal-path control
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hold finalization window
        run: sleep 45
      - name: Pass
        run: echo H1_BASEPIVOT_RECHECK_OK_20260812
