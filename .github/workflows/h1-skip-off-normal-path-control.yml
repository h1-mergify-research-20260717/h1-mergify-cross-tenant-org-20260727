name: H1 Skip-Off Normal Path Control

on:
  pull_request:

jobs:
  safety:
    name: H1 skip-off normal-path control
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hold validation window
        run: sleep 60
      - name: Pass
        run: echo H1_DEQUEUE_ATTACKER_AUTOPUSH_SLOW_OK_20260812
