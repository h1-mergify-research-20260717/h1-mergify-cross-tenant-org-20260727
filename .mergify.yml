merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
  auto_merge_conditions:
    - base = h1-fork-prov-headrepo-trust-target-20260812

merge_protections:
  - name: H1 head-repo trust boundary 20260812
    if:
      - base = h1-fork-prov-headrepo-trust-target-20260812
    success_conditions:
      - base = h1-fork-prov-headrepo-trust-target-20260812

queue_rules:
  - name: H1 head-repo trust boundary 20260812
    queue_conditions:
      - base = h1-fork-prov-headrepo-trust-target-20260812
    merge_conditions:
      - check-success = @github-actions/H1 head-repo provenance observer
      - check-success = @github-actions/H1 trusted same-repo publisher
      - label = H1_NEVER_FINAL_MERGE_HEADREPO_TRUST_20260812
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 2
  skip_intermediate_results: false
  queue_controls_comment: false
