commands_restrictions:
  queue:
    conditions:
      - sender = onecursor3-gif

queue_rules:
  - name: H1 queue-info git-note provenance 20260813
    queue_conditions:
      - base = h1-qnote-target-20260813
    merge_conditions:
      - check-success = H1_NEVER_QNOTE_MERGE_20260813
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 1
  skip_intermediate_results: false
  queue_controls_comment: false
