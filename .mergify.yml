commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max

queue_rules:
  - name: H1 checks timeout fail-closed 20260813
    queue_conditions:
      - base = h1-checks-timeout-target-20260813
    merge_conditions: []
    branch_protection_injection_mode: merge
    batch_size: 1
    allow_inplace_checks: false
    checks_timeout: 1 min
    max_checks_retries: 0
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 1
  skip_intermediate_results: false
  queue_controls_comment: false
