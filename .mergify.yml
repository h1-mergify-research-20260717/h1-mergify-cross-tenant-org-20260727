commands_restrictions:
  queue:
    conditions:
      - sender = onecursor3-gif

queue_rules:
  - name: H1 fork provenance ref integrity fresh 20260812
    queue_conditions:
      - base = h1-fork-prov-ref-target-20260812
    merge_conditions:
      - label = H1_NEVER_MERGE_FORK_PROV_REF_20260812
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 2
  skip_intermediate_results: false
  queue_controls_comment: false
